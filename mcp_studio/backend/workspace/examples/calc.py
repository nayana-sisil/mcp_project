"""Example module bundled with MCP Studio.

Replace me with your own files and try the tools, resources, and chat
in the UI to see how the MCP tools work end-to-end.
"""

from __future__ import annotations


def add(a: float, b: float) -> float:
    return a + b


def multiply(a: float, b: float) -> float:
    return a * b


if __name__ == "__main__":
    print(f"2 + 3 = {add(2, 3)}")
    print(f"2 * 3 = {multiply(2, 3)}")
